[gd_resource type="Resource" script_class="SkillDefinition" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/resources/skill_definition.gd" id="1_skilldef"]

[resource]
script = ExtResource("1_skilldef")
skill_id = "dps_ranger_trap_network_special"
display_name = "Trap Network"
skill_type = 3
max_level = 5
sp_cost_per_level = 1
description_template = "Place 1 trap: 30 dmg + 1s slow. 12s CD, 15s trap\nInitial: 1 trap / 30 dmg / 1s\nPer level: +0.5 trap / +5 dmg / +0.5s\nMax: 3 traps / 50 dmg / 3s"
value_per_level = "+0.5 trap / +5 dmg / +0.5s"
