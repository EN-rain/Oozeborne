[gd_resource type="Resource" script_class="SkillDefinition" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/resources/skill_definition.gd" id="1_skilldef"]

[resource]
script = ExtResource("1_skilldef")
skill_id = "controller_stormcaller_static_build_passive"
display_name = "Static Build"
skill_type = 2
max_level = 5
sp_cost_per_level = 1
description_template = "Each consecutive hit on controlled target adds +1-5% lightning damage, up to +5-25%\nInitial: +1% per hit / +5% max\nPer level: +1% per hit / +5% max\nMax: +5% per hit / +25% max"
value_per_level = "+1% per hit / +5% max"
